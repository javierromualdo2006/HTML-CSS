const lista = [1,2,4,5,5,6,6,7] 

function sumarPares(numeros) {

let i = 0 
while (i < numeros.length) {
    console.log(numeros[i]) // imprime el elemento en la posición i
    i++ // incrementamos el índice en 1 para la siguiente iteración

 }

}
console.log(sumarPares(numeros))

                                            